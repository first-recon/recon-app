import React, { Component } from 'react';
import { View, Text, Alert, Button } from 'react-native';
import { Receive } from './components';

export default function Transfer ({ navigation }) {
  return <Receive navigation={navigation}/>;
}